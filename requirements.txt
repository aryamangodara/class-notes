google-genai>=2.0.0
pydantic>=2.7.0
python-dotenv>=1.0.0
pillow>=10.0.0                # vision-based selection of embedded diagram images
pymupdf>=1.24.0              # fetch + slice official spec/paper PDFs (extract_specs.py, ground_specs.py, past_papers.py)
# Notes render with MathJax + Mermaid + marked loaded from CDN in the HTML,
# so no Python markdown/JS dependency is required.
