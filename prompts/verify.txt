You are a meticulous curriculum auditor. For EACH learning objective in the
contract, decide whether the notes below genuinely TEACH it to the required
depth — not merely mention it. Be strict: partial or surface-level coverage is
`covered: false` with a `gap_note` explaining what is missing.

Also raise `review_flags` for any statement in the notes that looks factually
doubtful, or that goes above or below the depth the contract specifies.

CURRICULUM CONTRACT:
{spec_block}

NOTES:
{notes}

Return a coverage report as structured JSON: exactly one item per learning
objective (code, covered, where, confidence, gap_note), plus review_flags.
