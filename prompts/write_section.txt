{house_style}

You are writing ONE section of class notes, grounded in the curriculum contract
below. Write only this section — do not teach other sections' objectives.

CURRICULUM CONTRACT (for context — calibrate depth to this):
{spec_block}

THIS SECTION:
  Heading: {heading}
  Intent:  {intent}
  Must teach these objectives: {codes}

THE FULL OUTLINE (the other sections — do NOT teach their material; each
objective is owned by one section, so do not repeat what another section covers,
though you may refer to it briefly):
{outline}

THIS BOARD'S EXAM FORMAT (accurate, board-general facts — use them to keep your
section exam-strategy pointers consistent with how this exam actually works):
{exam_format}

Guidance:
- Write the section body in clear, level-appropriate Markdown with real line breaks.
- Inline maths as \(...\), display maths as $$...$$. Write currency as a plain
  dollar sign (e.g. $80), never inside maths.
- Present any table as a Markdown table — never as a LaTeX table (no tabular,
  array, or hline environments; MathJax cannot render them).
- Include worked examples where they genuinely aid understanding.
- Add diagrams where they help: `mermaid` for processes/cycles/flows AND schematic
  node/arrow diagrams you can draw that way — including reaction energy profiles and
  reaction-coordinate diagrams; `latex` for a single expression or equation ONLY
  (never a table); `image` to pull a real labelled diagram, photo, micrograph, or map
  from a free image library — set `content` to a SHORT keyword query of 2-4 words
  naming the thing itself (e.g. "chloroplast diagram", "mitochondrion structure",
  "reaction energy profile"), NOT a full sentence, and give a caption. Prefer `image`
  for STANDARD, widely-depicted things that free libraries reliably have — labelled
  organelles, cells, atomic/bonding diagrams, standard apparatus (e.g. an electrolysis
  cell), periodic trends. Use `description` ONLY as a last resort for a bespoke, one-off
  illustration that has no mermaid or image form: it is a teacher/illustrator stub that
  is NOT shown to students, so never rely on it to convey something a student needs —
  teach that in the body, or draw it with mermaid/image. Use diagrams where a real
  visual genuinely helps, not decoratively.
- Include the STANDARD diagram a student must be able to read or draw for THIS
  section's objectives, as a real `mermaid` or `image` — e.g. an exothermic/
  endothermic reaction energy profile (progress of reaction on the x-axis, energy on
  the y-axis; reactant and product levels, activation energy and delta H labelled) for
  a section on energy changes. A brief visual recap of a foundational idea is worth
  including even if it is listed as a prerequisite; never leave a core visual implicit
  or prose-only.
- In `mermaid` diagrams, wrap any node label containing parentheses, `+`, or
  punctuation in double quotes (e.g. `A["Glucose (6C)"]`) or Mermaid fails to parse.
- Do not write the internal objective codes (e.g. SAT-ALG-LIN2-1) in the notes;
  they are references for you only.
- Match the board's expected rigour exactly — do not exceed the depth profile.
- Add `callouts` where they genuinely help: a Quick Tip (`tip`), a Common Mistake
  (`mistake` — the error AND its fix), a Key Formula or key fact (`formula`), or a
  Remember/mnemonic (`remember`). Keep each to 1-3 sentences, make them
  subject-appropriate, and include one ONLY when it is accurate and grounded —
  omit rather than invent. Do not force one of every kind or just repeat the body.
- Add `exam_tips`: 1-3 pointers on how THIS section's content is examined — the
  command words used, what earns or loses marks, and frequent errors — specific to
  this subtopic and consistent with the board's exam format above. Omit if the
  section has no distinctive exam angle; do not restate generic board-wide rules.
- Set `confidence` honestly; if any claim is uncertain, lower it.

Return the section as structured JSON.
