# Auth — identical contract to the Grader, so the same key works here.
# Pick ONE method.

# AI Studio (simplest):
GEMINI_API_KEY=

# OR Vertex AI:
# GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/service-account.json
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=global
